# 奉納ビラ 印刷＆名簿管理システム CHANGELOG

すべての大切な変更はここに記録されます。

> **注記**: v41〜v83 の変更はこのファイルに記録されていません。今後はリリースごとに1行でも追記してください。
> リリース時は `index.html` の `app.js?v=` 、`sw.js` の `CACHE_NAME` と `ASSETS_TO_CACHE` 内の `app.js?v=` 、`app.js` の `APP_VERSION` 、`index.html` フッターの表示バージョンの**5箇所**を必ず揃えてください。

## [v84] - 2026-09-03

### Security (セキュリティ)
- **GAS共有トークン認証**: 公開URLを知る第三者が名簿スプレッドシートへ書き込み・削除できる状態を塞ぎました。設定画面の「共有トークン」に入力した値を、GETは `auth` クエリ、POSTは body の `auth` として送り、GAS側のスクリプトプロパティ `SHARED_TOKEN` と照合します。**GAS側が未設定なら検証をスキップする**ため、移行中に既存端末が止まることはありません（手順は `GAS更新指示書.md` 参照）。
- 認証エラー時は `gasAuthBlocked` フラグで同期を停止し、無限リトライを防ぎます。未送信データは破棄せずキューに残ります。
- トークンは localStorage ではなく IndexedDB の `kv` ストア（キー `gas_shared_token`）に保存します。端末識別用の既存 `token` / `api_token` とは別物です。
- `.gitignore` に `.clasp.json`（GASのスクリプトIDを含む）、開発メモ、未使用の旧テンプレートPDFを追加しました。退避用に `_cleanup_dev_files.bat` を同梱しています。

### Fixed (バグ修正)
- **一括印刷・選択分CSV・選択削除が動作しない**: v40で実装済みとしていたこれらの機能は、`index.html` と `renderTable()` にチェックボックス列・操作ボタンが一切存在せず、**到達不能なデッドコードのままでした**。選択UIを新設して接続しました。
- **漢数字の大字「拾」で金額が10分の1に過小計上される**: `壱`〜`伍` は対応済みなのに `拾` が単位表に無く、文字フィルタで除去されるため「参拾萬圓」が300,000円ではなく30,000円として集計されていました（0円ではありません）。`拾` `什` `陌` `佰` を追加しました。
  - **注意**: このリリースを適用すると、過去に登録済みの「拾」を含む金額の集計値が本来の10倍に跳ね上がります。ダッシュボードの合計額が急増して見えるのは正常な挙動（バグの修正）であり、リリース前にスタッフへ事前周知してください。
- **「GASコードをコピー」が古いコードを配っていた**: `app.js` に埋め込まれていたGASコードが実物より古い実装（ハードコード列参照）で乖離していました。`gas_script.js` の実ファイルを直接読む方式に変え、二重管理そのものを解消しました。クリップボードが使えない環境向けの手動コピー用モーダルも追加しています。
- **`letter.html` がオフラインで開けない**: Service Worker のキャッシュ対象に入っていませんでした。`gas_script.js` `templates_config.json` と併せて追加し、404になっていた `fa-solid-900.ttf` は削除、不足していた `fa-regular` / `fa-brands` の woff2 を追加しました。
- **`onFontChange` の二重定義**: 後の定義が前を黙って上書きしていました。旧い簡素版を削除しました。
- `escapeHtml` / `escapeHTML` の重複を `escapeHTML` に一本化しました。
- 初回ダウンロードの案内が「8MB程度」でしたが実際は約5MBなので表記を修正しました。
- `templates_config.json` が誰にも読まれず `getFallbackConfig()` だけが実質の設定だった二重管理を、JSONを正本・関数をフォールバックとする形に整理しました。

- **起動中に「準備完了」と誤表示される**: 初回セットアップ中（フォント・台紙PDFのダウンロード中）に `updatePreview()` と `getTemplateBytes()` がヘッダーを一瞬「準備完了」にしていました。まだ印刷できない状態なのに完了表示が出るため、利用者が印刷を押して失敗する原因になります。準備が整うまで表示しないようにしました。
- **初回ダウンロードの再試行とタイムアウト**: 通信が1回失敗しただけで手動セットアップ画面に落ちていました。指数バックオフで3回まで再試行します。あわせて45秒のタイムアウトを設け、応答が返らないまま「初回セットアップ中...」で永久に止まる状態を解消しました。

### Added (追加機能)
- **アプリ更新通知**: 新しいバージョンを検知すると「新しいバージョンがあります／再読み込み」トーストを常駐表示します。30分ごとに更新を確認します。
- **同期ステータスの常時表示**: ヘッダーに最終同期日時（相対表示、ホバーで絶対時刻）と未送信件数、オフライン表示を出します。
- **一括印刷の進捗バーと中断**: 処理件数のバー表示と中断ボタンを追加しました。中断時はそこまでの分を印刷するか選べます。
- **プレビューの部分拡大**: 「氏名」「金額」ボタンで該当欄を拡大表示し、印字位置の微調整を確認しやすくしました。
- **名簿からの再印刷**: 各行の再印刷ボタンで、そのレコードのPDFを**現在のデザイン設定で**作り直せます。
- **集計の期間切替と前年同期比較**: 今日／今月／今年／全期間／期間指定で絞り込め、前年同期との増減率を併記します。初期値は「全期間」なので既存の見え方は変わりません。
- **読み仮名（任意）**: 漢字名を読みで検索できるようにしました。既存レコードへの一括付与は行いません。CSV・スプレッドシートとも同期します（シートに「読み仮名」列が無ければ従来通り動作）。
- **「今日の分に御礼状」**: ヘッダーから `letter.html?filter=today` へ遷移し、今日登録された奉納者だけを全選択済みの状態で開きます。
- **自動テスト**: `npm test` でユニットテスト56件（漢数字パース・住所正規化・決定的ID生成・縦書き分割・日付パースなど）、`npm run test:e2e` で起動〜プレビュー描画のスモークテストが走ります。`app.js` は分割していないため、テストは app.js から対象関数だけを抽出して評価する方式です（`tests/README.md` 参照）。

### Changed (変更点)
- Service Worker の `install` から `skipWaiting()` を外し、更新は利用者が「再読み込み」を押したときに適用されるようにしました（作業中に突然新版へ切り替わるのを防ぐため）。
- キャッシュバージョンを v84 に統一しました。

## [v40] - 2026-07-20
### Fixed (バグ修正)
- **集計ダッシュボード**: テンプレート別の印刷枚数が常に0枚と表示される問題を修正しました（レコードのキー参照誤り）。
- **キャッシュ強制クリア**: 「最新ファイルを強制再ダウンロード」が実際にはキャッシュを消していなかった問題を修正し、Service Workerキャッシュも同時にクリアするようにしました。
- **未送信キュー管理モーダル**: 旧localStorageキュー方式の残骸により全く動作していなかったモーダルを、IndexedDB Outbox対応で全面的に書き直しました（削除待ちの表示・個別送信・除外に対応）。
- **削除の同期（墓標方式）**: オフラインで削除したデータがスプレッドシートに残り、「名簿フル復元」で復活してしまう問題を修正。削除も未送信キューに積まれ、オンライン復帰時にクラウドへ伝搬されます。
- **名簿フル復元の増殖**: ID列が空の行を復元するたびにデータが二重登録される問題を修正（行内容から決定的なIDを生成）。
- **復元データの日付**: スプレッドシート由来の日付形式がSafari等でパースできず日時表示が壊れる問題を修正。
- **「空」タグの混入**: 名簿からの呼び出し・一括印刷時に「[空]」がそのまま印字される問題を修正。空チェックの状態もフォームに復元されます。
- **GAS送信の成否確認**: no-cors送信で失敗を検知できず「送信済み」扱いになっていた問題を修正。GASの応答を確認できた場合のみ同期済みにします。
- **プレビューの競合**: 連続入力時に古いプレビューが後から上書き表示されることがある問題を修正（世代トークン導入）。
- **横書きの自動縮小**: 字間設定時にフォント縮小後も枠をはみ出すことがある計算式の誤りを修正。
- **並び順**: リロード後に名簿の並び順が変わる問題を修正し、並び替えセレクト（日付順・台紙種類順・氏名順）を実装しました。
- **漢数字パース**: 「十萬」「壱阡五百」などの複合表記が正しく集計されない問題を修正。
- **長押しリピート**: 存在しない要素を対象にしていたため動作していなかった＋/−ボタンの長押しリピートを実装し直しました。
- **サジェスト候補の削除**: 削除してもリロードで復活する問題を修正（IndexedDBへ反映されていなかった）。
- **起動時の名簿表示**: 起動直後に名簿が空で表示されることがある問題を修正。
- **メモリリーク**: 印刷・保存時のBlob URLが解放されない/早すぎるタイミングで解放される問題を修正。

### Added (追加機能)
- **選択分CSV・選択削除**: ボタンのみ存在し未実装だった「選択分CSV」「選択削除」を実装しました。
- **PWAアイコン**: PNGアイコン（180/192/512px）を追加し、iOSのホーム画面アイコン（apple-touch-icon）に対応しました。

### Changed (変更点)
- キャッシュバージョンを v40 に統一しました（index.html の app.js?v= と sw.js の CACHE_NAME を必ず揃える運用に）。
- Service Worker がエラー応答（404等）をキャッシュしないようにし、Google FontsをオフラインでもUIに供給できるようキャッシュ対応しました。
- XSS対策として、名簿テーブル・サジェストリストのインラインonclickをイベントリスナー方式に変更しました。
- 未実装のまま表示されない「集計ダッシュボードタブ」（ID重複あり）などのデッドコードを削除しました。
- ピンチズームを禁止していたviewport設定を解除しました（アクセシビリティ向上）。

## [v39] - 2026-07-09
### Added (追加機能)
- **データの編集機能**: 登録済みの名簿データをフォームに読み込み、金額や氏名を修正して更新できるようになりました。
- **スプレッドシート連動削除**: アプリ上で名簿データを削除した際、オンライン状態であれば自動的にスプレッドシート（GAS経由）上の該当行も削除されるように対応しました。

## [v38] - 2026-06-10
### Added (追加機能)
- **オフラインファースト対応**: インターネット未接続時でもアプリの起動・PDF生成・名簿保存が可能になりました。
- **Outbox同期パターン**: オフライン時の名簿保存を「未送信」キューとしてIndexedDBに溜め、オンライン復帰時(`online`イベント検知)に自動でバックグラウンド送信する機能を実装しました。
- **排他制御と冪等性**: 複数端末からの同時送信によるスプレッドシート破損を防ぐため、GASバックエンドに `LockService.getScriptLock()` を導入。また、再送による重複を防ぐためIDに基づく上書き更新処理（冪等化）を実装しました。
- **永続ストレージの要求**: `navigator.storage.persist()` を呼び出し、iOS Safari等による勝手なキャッシュ・データクリアを防止する仕組みを導入しました。
- **名簿フル復元機能**: スプレッドシートからこれまでの履歴を全件ダウンロードしてローカルに復元する機能（`?mode=restore`）を追加しました。
- **ファイルバックアップ機能**: 名簿履歴をJSONファイルとしてローカルに保存・読み込みする機能を設定画面に追加しました。

### Changed (変更点)
- メインストレージを `localStorage` から `IndexedDB (v2)` に完全移行しました。
- CDNで読み込んでいた外部ライブラリ（pdf.js, pdf-lib, fontkit, FontAwesome）をすべてローカルの `lib/` フォルダに取り込み、オフラインでも表示崩れが起きないようにしました。
- PWAの Service Worker キャッシュバージョンを `v38` に更新しました。

### Fixed (修正点)
- FontAwesome アイコンが文字化け（☒）する問題を修正（CSS内のフォントパスを `../webfonts/` から `./webfonts/` に変更）。
