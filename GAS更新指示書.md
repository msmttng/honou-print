# GAS（Google Apps Script）更新・再デプロイ 指示書

## 目的
奉納ビラ印刷アプリに「奉納袋番号」「住所」機能を追加した。
これに伴い、スプレッドシート連携用のGAS（プロジェクト名: **奉納ビラ管理台帳**）を更新し、
「履歴」シートに **奉納袋番号** と **住所** の2列を保存できるようにする。

**アプリ本体（index.html / app.js / sw.js）はすでにGitHubへpush済み。この作業はGAS側のみが対象。**

---

## 対象ファイル（変更済み・これが正）
- リポジトリ内の **`gas_script.js`**（このファイルがGASの最新ソース）

このファイルは既に修正済みなので、内容を再検討する必要はない。
**`gas_script.js` の中身を、対象のApps Scriptプロジェクトに反映（上書き）し、新バージョンとして再デプロイすること。**

---

## 反映すべき変更点（要約 / すでに gas_script.js に反映済み）
「履歴」シートの列を A〜F から A〜H に拡張し、G列=奉納袋番号 / H列=住所 を追加した。

1. **doPost（書き込み）**
   - リクエストから `bagNo`（奉納袋番号）と `address`（住所）を受け取る。
   - 新規シート作成時のヘッダを8列に：`["日時","台紙種類","奉納者氏名","金額/物品名","ID","Token","奉納袋番号","住所"]`
   - 既存シートにG1/H1ヘッダが無ければ自動で補う処理を追加。
   - 新規追加・冪等更新ともに8列で書き込む（G=bagNo, H=address）。
   - **ID列はE列（5列目）のまま**変更していない（削除・重複判定ロジックは従来通り動く）。

2. **doGet（mode=restore / 復元）**
   - 取得範囲を5列→8列に拡張し、返すレコードに `bagNo`（row[6]）と `address`（row[7]）を追加。

3. **doGet（サジェスト取得・通常時）**
   - 変更なし。

※ 既存の200件超のデータは削除・移動しない。G/H列が空のまま残るだけで、以降の登録で埋まっていく。

---

## デプロイ手順（重要：URLを変えないこと）

**現在の公開URL（このまま維持する）:**
`https://script.google.com/macros/s/AKfycbxVFWGyZTgPPVDo430RzF3QCjuS7qYHGtjifv_KK6clkVUB0zVHYd5d-k9Gw9nGNcNc/exec`

新規デプロイを作るとURLが変わってしまうため、**既存デプロイを「新バージョン」で更新する**こと。

### 方法A：clasp（コマンドライン）が使える場合
1. 対象のApps Scriptプロジェクトにclaspがリンクされていることを確認（`.clasp.json` の scriptId が上記デプロイと同一プロジェクトであること）。
2. `gas_script.js` の内容をclasp管理下の該当ファイル（例: `Code.gs`）へ反映する。
3. `clasp push` でコードをアップロード。
4. 既存デプロイを新バージョンで更新（URL維持）：
   `clasp deploy -i AKfycbxVFWGyZTgPPVDo430RzF3QCjuS7qYHGtjifv_KK6clkVUB0zVHYd5d-k9Gw9nGNcNc -d "番号・住所対応"`
   ※ `-i` に指定するのは上記URL内のデプロイIDと同じ文字列。

### 方法B：Apps Scriptエディタ（ブラウザUI）が使える場合
1. Apps Scriptエディタで「奉納ビラ管理台帳」プロジェクトを開く。
2. 既存コードを全削除し、`gas_script.js` の内容を全て貼り付けて保存。
3. 右上「**デプロイ**」→「**デプロイを管理**」を開く。
4. 対象のウェブアプリ行の**鉛筆（編集）アイコン**をクリック。
5. 「バージョン」を「**新バージョン**」に変更。
6. 「次のユーザーとして実行」=**自分**、「アクセスできるユーザー」=**全員** を確認。
7. 「**デプロイ**」をクリック（この方法ならURLは変わらない）。

---

## 動作確認（デプロイ後）
1. ブラウザで上記 `…/exec` URLを開き、JSON（`{"names":[...],"items":[...]}`）が表示されることを確認。
2. アプリ `https://msmttng.github.io/honou-print/` を開き（スーパーリロード Ctrl+Shift+R）、氏名・住所・奉納袋番号を入れて1件テスト印刷。
3. スプレッドシートの「履歴」シートに、**G列=奉納袋番号 / H列=住所** が入った行が追加されていることを確認。
4. G1/H1のヘッダが「奉納袋番号」「住所」になっていることを確認。

---

## 制約・注意
- 公開URL（/exec）は**絶対に変えない**（アプリ側が同URLに送信しているため）。
- 「アクセスできるユーザー」は必ず「**全員**」（でないとアプリからの保存が失敗する）。
- 既存データ（A〜F列）の並び・ID列（E列）の位置は変更しない。
- スクリプトはスプレッドシートに紐づいた（コンテナバインド）状態を維持すること（`SpreadsheetApp.getActiveSpreadsheet()` を使用しているため、スタンドアロン化しない）。

---

## 追記：共有トークン認証（SHARED_TOKEN）と読み仮名（kana）列の追加

### 1. 共有トークン（`SHARED_TOKEN`）の設定手順

第三者が公開URLを知っていても書き込み・読み取りできないようにするため、共有トークンによる簡易認証を追加した。
GAS側は `params.auth`（POST）／ `e.parameter.auth`（GET）を、スクリプトプロパティ `SHARED_TOKEN` と照合する。
`SHARED_TOKEN` が**未設定の間は認証をスキップする**（＝今まで通り誰でもアクセス可能）。設定して初めて有効化される。

**設定手順:**
1. Apps Scriptエディタで対象プロジェクトを開く。
2. 左メニューの「**プロジェクトの設定**」（歯車アイコン）を開く。
3. 「**スクリプト プロパティ**」セクションで「プロパティを追加」をクリック。
4. プロパティ名に `SHARED_TOKEN`、値に任意の秘密トークン文字列を入力して保存。

**注意:** 既存の `token`（GUEST_TOKEN、端末識別用でシートのToken列に記録される値）とは別物。共有トークンは必ずリクエストパラメータ名 `auth` で送る。

### 2. 移行手順（必ずこの順序で実施すること）

順序を間違えると、トークンを配布し終える前に締め出しが発生し、業務が止まる恐れがある。**必ず以下の順で実施すること。**

1. この新しい `gas_script.js` をデプロイする（**`SHARED_TOKEN` は未設定のまま**）。この時点では認証がスキップされるため、全端末が今まで通り動く。
2. 新しいアプリ（app.js / index.html）をリリースする。トークン未入力の端末も（GAS側が未設定のため）素通しで動く。
3. 各スタッフ端末の設定画面「共有トークン」欄にトークンを入力して回る。
4. **全端末への投入を確認してから**、GASの `SHARED_TOKEN` を設定する。ここで初めて、トークンを持たない第三者が締め出される。
5. （任意）新URLでデプロイし直し、旧デプロイをアーカイブして、旧URLを知る第三者を完全に切り離す。

### 3. ロールバック方法

`SHARED_TOKEN` スクリプトプロパティを削除するだけで、上記手順3以前の状態（認証スキップ）に戻る。アプリ側のコード変更は不要。

### 4. 読み仮名（kana）列について

読み仮名機能を使う場合は、スプレッドシート「履歴」シートのヘッダー行（1行目）に「**読み仮名**」という見出しの列を追加する必要がある（見出し名は「ふりがな」「カナ」「よみ」のいずれでも自動認識される）。
この列を追加しなければ、読み仮名は保存・復元されない（空欄のまま）だけで、それ以外の機能は今まで通り動作する。

**必須手順**: 読み仮名を使うなら、先にスプレッドシートへ『読み仮名』列を追加してください。列が無い状態で『名簿フル復元』を実行すると、端末に入力済みの読み仮名が消えます（アプリ側で確認ダイアログは出るが、列追加を後回しにしないこと）。
